-- [XI] 인덱스 : 조회를 빠르게 하는 인덱스
SELECT * FROM USER_INDEXES;

DROP TABLE EMP01;

CREATE TABLE EMP01 -- EMP 테이블과 같은 내용의 TABLE
    AS SELECT *
        FROM EMP;

SELECT * FROM EMP01; -- 14행

INSERT INTO EMP01 SELECT * FROM EMP01; -- ★ 데이터뻥튀기 1번 (28) 2QJS (56행)
INSERT INTO EMP01 SELECT * FROM EMP01; -- ★ 11번 실행 (11만개)
INSERT INTO EMP01 (EMPNO, ENAME, DEPTNO) VALUES (1111, 'HONG', 40);
INSERT INTO EMP01 SELECT * FROM EMP01; -- 1번 (22만) 2번 (44만) 3번 (90만)

SELECT TO_CHAR (COUNT(*), '9,999,999') 
    FROM EMP01;
    
-- 인덱스 생성 전 조회 --
SELECT * FROM EMP01 
    WHERE ENAME = 'HONG'; -- 인덱스 생성전 조회 시간

--인덱스 생성
CREATE INDEX IDX_EMP01_ENAMM -- 0.658초
    ON EMP01 (ENAME);

-- 인덱스 생성 후 조회 --
SELECT * FROM EMP01 
    WHERE ENAME = 'HONG'; -- 인덱스 생성 후 조회시간 0.001초
COMMIT;

INSERT INTO EMP01 SELECT * FROM EMP01; -- 인덱스 생성 후 91만 -> 182만개로 INSERT 시간 : 32.016초 30.52
SELECT TO_CHAR (COUNT(*), '9,999,999') 
    FROM EMP01;
    
ROLLBACK; -- 12.099초

DROP INDEX IDX_EMP01_ENAMM;

INSERT INTO EMP01 SELECT * FROM EMP01; -- 인덱스 제거후 91만 -> 182만개로 INSERT 시간 : 3.077초

SELECT * FROM USER_INDEXES 
    WHERE TABLE_NAME = 'EMP01';
    
DROP TABLE EMP01; -- 테이블을 지우면 해당 테이블의 인덱스는 자동 삭제